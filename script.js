const avanca = document. querySelectorAll('.btn-proximo');

avanca.forEach(button =>)  {
       button.addEventlistener('click , fuction(){
        const atual = document.querySelector('.ativo');
        const proximopasso = 'passo-' + this.getattribute('data-')
